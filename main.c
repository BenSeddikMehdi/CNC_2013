#include <stdio.h>
#include "First_Problem.h"
#include "Second_Problem.h"

int main() {
    /*point_t AA = {0, 2}, BB = {3, 5};
    horizontalPath(AA,BB);
    for (int i = 0; i < Max; ++i) {
        printf("C[%d] = P(%d,%d)\n", i, C[i].x, C[i].y);
    }*/
    printf("distance = %d\n", distance(3));
    return 0;
}